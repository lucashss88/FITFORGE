class DashboardController < ApplicationController
  def index
    # @last_workout = current_user.workouts.last
    # @exercise_details = @last_workout.exercises.includes(:exercise_sessions) if @last_workout
  end
end
